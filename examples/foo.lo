#c.'#include <stdio.h>'

// #ts.@alias('split')
fun (s string) lo_split(delim string) []string {
	return []string
}

// TODO return type
pub fun ini_decode(s string) {
	lines := s.lo_split("\n")
	// #ts.'console.log(lines)'
	// #c.'printf("hi\n");'
}

@main
fun my_main() {
	ini_decode("a\nb\nc")
}
