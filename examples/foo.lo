#c.'#include <stdio.h>'

// TODO return type
pub fun ini_decode(s string) {
	lines := s.split("\n")
	for i := 0; i < lines.length; i += 1 {
		line := lines[i]
		// TODO &&, ||
		// TODO string.starts_with
		if line.length == 0 {
			continue
		}
	}
}

@main
fun my_main() {
	ini_decode("[section]
; my comment")
}
